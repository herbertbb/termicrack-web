---
deployment:
  tasks:
    - export DEPLOYPATH=/home/termicrack/public_html
    - export REPOPATH=/home/termicrack/repositories/termicrack-web
    - /usr/bin/rsync -av --exclude='.git/' --exclude='cgi-bin/' --exclude='api/' --exclude='ventas/' --exclude='trabajos/' ${REPOPATH}/ ${DEPLOYPATH}/